<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="utf-8">
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <meta name="viewport" content="width=device-width, initial-scale=1">

        <title><%= title %></title>
        <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">
        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/fancybox/3.1.20/jquery.fancybox.min.css" />
        <link rel="stylesheet" href="/css/style.css" />
        <link href="https://fonts.googleapis.com/icon?family=Material+Icons"
        rel="stylesheet">
        <script src="https://ajax.googleapis.com/ajax/libs/jquery/1.12.4/jquery.min.js"></script>
    </head>

    <body>
        <nav class="navbar navbar-inverse" style="background-color:palevioletred; width:102.3%; height:70px;margin-left: -13px; margin-top:-14px; border:hidden;">
            <div class="container">
               <a href="http://localhost:4200/products" style="text-decoration: none; font-size: 29px; color:whitesmoke;">GafferCart</a>
                <div id="navbar" class="collapse navbar-collapse">
                    </div><!--/.nav-collapse -->
               
            </div>
        </nav>
                    <ul class="nav navbar-nav navbar-right">
                        <a href="/cart/checkout" style="text-decoration: none; color:#222; margin:50px; "><i class="material-icons">
                            shopping_cart
                            </i>(
                    <% if(typeof cart!=="undefined"){ %>
                        <%= cart.length %>
                        <% } else {%>
                            0
                   <% } %>
                            )
                        </a>
                    
           
                </ul>


        <div class="container">
            <%- messages('messages', locals) %>
            
            <% if (errors) {%>
                <% errors.forEach(function(error) { %>
                <div class="alert alert-danger">
                    <%= error.msg %>
                </div>
                <% });%>
            <% }%>
    
            
        <div class="container">
            <%- messages('messages', locals) %>
            
            <% if (errors) {%>
                <% errors.forEach(function(error) { %>
                <div class="alert alert-danger">
                    <%= error.msg %>
                </div>
                <% });%>
            <% }%>

            
                <div class="col-xs-12 col-md-1"></div>
                <div class="col-xs-12 col-md-8">