<%- include('../layouts/adminheader')  %>

<h2 class="page-title">Pages</h2>
<a href="/admin/pages/add-page" class="btn btn-primary">Add a new page</a>
<br /><br />
<!--

<table class="table table-striped">
  <thead>
    <tr>
      <th>Title</th>
      <th>Edit</th>
      <th>Delete</th>
    </tr>
  </thead>
  <tbody>

  </tbody>
</table> -->

    <div class="content">
        <div class="container-fluid">
            <div class="row">
                <div class="col-md-12">
                  <a href="/admin/pages/add-page" class="btn btn-primary">Add a new page</a>
                    <div class="card">
                        <div class="card-header" data-background-color="purple">
                            <h4 class="title">All Pages</h4>
                        </div>
                        <div class="card-content table-responsive">
                            <table id="table" class="table"  cellspacing="0" width="100%">
                                <thead class="text-primary">
                                <th>Title</th>
                                <th>Created At</th>
                                <th>Updated At</th>
                                <th>Action</th>
                                </thead>
                                <tbody>
                            <% pages.forEach(function(page) { %>
                                        <tr>
                                            <td><%= page.title %></td>
                                            <td><%= page.createdAt %></td>
                                            <td><%= page.updatedAt %></td>
                                            <td>
                                                <a href="/admin/pages/edit-page/<%= page.slug %>" class="btn btn-info btn-sm"><i class="material-icons">mode_edit</i></a>
                                                <% if (page.slug == "home") { %>
                                                  <td></td>
                                                  <% } else {%>
                                                    <a href="/admin/pages/delete-page/<%= page.slug %>" class="btn btn-info btn-sm"><i class="material-icons">delete</i></a>
                                                    <% } %>
                                                  </tr>
                                                  <% }); %>

                                                  //For Delete Function
                                                <!-- <form id="delete-form-{{ $item->id }}" action="{{ route('item.destroy',$item->id) }}" style="display: none;" method="POST">
                                                </form>
                                                <button type="button" class="btn btn-danger btn-sm" onclick="if(confirm('Are you sure? You want to delete this?')){
                                                    event.preventDefault();
                                                    document.getElementById('delete-form-{{ $item->id }}').submit();
                                                }else {
                                                    event.preventDefault();
                                                        }"><i class="material-icons">delete</i></button>
                                            </td>
                                        </tr> -->

                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
        <%- include('../layouts/adminfooter')  %>
